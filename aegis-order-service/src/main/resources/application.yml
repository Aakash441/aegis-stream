server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres-order:5432/aegis_order
    username: postgres
    password: Aakash@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  redis:
    host: redis
    port: 6379
  kafka:
    bootstrap-servers: kafka:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
jwt:
  secret: this-project-will-get-me-into-big-tech
  expiration: 86400000

logging:
  level:
    org.hibernate.SQL: DEBUG
